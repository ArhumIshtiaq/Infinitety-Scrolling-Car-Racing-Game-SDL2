#pragma once
#include <SDL.h>
#include <SDL_image.h>
#include "Game.hpp"

class Menu
{
    public:
        SDL_Surface* background;
        void show(SDL_Surface*);
};

void Menu::show(SDL_Surface* screen)
{
    
}
